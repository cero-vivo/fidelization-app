import { IEcommerceProductGateway } from "./IEcommerceProductGateway";

export const ecommerceProductGateway = () : IEcommerceProductGateway => {
    return {
        getProductById(id) {
            
        },
        listProducts(filter) {
            
        },
        createProduct(product) {
            
        },
        updateProduct(productId, updates) {
            
        },
        deleteProduct(productId) {
            
        },
    }
}